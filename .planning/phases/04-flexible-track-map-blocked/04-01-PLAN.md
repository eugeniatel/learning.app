# 04-01 Plan: Flexible Map and Manual Phase Toggle

## Goal

Finish Phase 4 by exposing the flexible curriculum as a navigable map and making checkpoint 3 a manual phase toggle.

## Decisions

- Checkpoint 3 trigger is manual.
- Toggle lives on This Week.
- Toggle is one-click and bidirectional.
- Branches are markdown modules with `track: branch` and `branchOf`.
- Seed branches are agent systems, domain eval lab, and small model efficiency.

## Tasks

1. Extend module frontmatter parsing to support `track: branch` and `branchOf`.
2. Add branch markdown files and register them in `curriculum/spine.json`.
3. Add a `/flexible-map` route with main spine, interpretability rail, branch cards, active/completed/ready/locked node states, and ready-to-pull cards.
4. Add a manual phase toggle on This Week backed by a server action writing `progress.json.phase`.
5. Seed the core 15-week schedule from the existing spine while preserving current mutable progress.
6. Reconcile planning docs and verify with lint/build.
